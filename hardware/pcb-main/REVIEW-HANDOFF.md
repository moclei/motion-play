I need a thorough final review of my main PCB design before sending it to JLCPCB for manufacture and assembly. The goal is to catch any errors that would result in a non-functional board, wasted money, or a board that needs rework.

### Context

Read these files first (in this order):

1. `/Users/marcocleirigh/Workspace/motion-play/.context/PROJECT.md` — project overview
2. `/Users/marcocleirigh/Workspace/motion-play/hardware/CONTEXT.md` — hardware architecture
3. `/Users/marcocleirigh/Workspace/motion-play/hardware/pcb-main/kicad/circuit-context.json` — freshly extracted schematic data (all components, nets, pins, functional blocks)
4. `/Users/marcocleirigh/Workspace/motion-play/hardware/pcb-main/kicad/pcb-layout-context.json` — freshly extracted PCB layout data (component positions, board outline, pad locations)

### Recent changes since last preflight review (2026-03-16)

These changes have NOT been reviewed yet:

1. **Removed T-Display-S3 dev board**, replaced with custom ESP32-S3-WROOM-1 implementation (U3) on a new `esp32_s3.kicad_sch` sub-sheet. Includes boot/reset buttons (SW2, SW3), USB data resistors (R52, R53), EN pull-up (R50), bootstrap cap/decoupling (C50-C55), status LED (D5 WS2812B-MINI), and GPIO strapping.
2. **Removed DWEII USB-C power module**, replaced with custom power circuitry: BQ24195 battery charger/PMIC (U5), TPS61088 5V boost converter (U6), INA219 current monitor (U7), and supporting components.
3. **Reverse polarity protection fix**: R34 was rewired from BAT_RAW to GND. Previously R33 (VBAT→GATE) and R34 (GATE→BAT_RAW) formed a non-functional divider. Now R34 (GATE→GND) creates a proper divider giving Vgs ≈ -VBAT/2.
4. **Added power indicator LED**: D1 (green LED) + R1 (1kΩ) on the 3.3V rail.
5. **Replaced JST-PH battery connector (J8) with Keystone 1042 battery holder (BT1)**: SMD 18650 holder on the back of the PCB. Footprint modified — one NPTH mechanical hole deleted (conflicted with front-side power components), another deleted (couldn't relocate). Two solder pads + one remaining peg retained.
6. **Added AMS1117-3.3 LDO (U8)**: Additional 3.3V regulator (check if this is redundant with AP2112K).
7. **Added USBLC6-2SC6 (U9)**: USB ESD protection.
8. **Added power switch SW1** (SS-12D10L5): Physical on/off switch.
9. **Board width changed**: Was 88mm, narrowed, then re-widened to ~88mm to fit the battery holder.

### What to review

#### 1. Schematic correctness (use circuit-context.json)
- Verify all power rails connect correctly: VBUS_RAW → protected VBUS → BQ24195 → VSYS → TPS61088 → +5V; VSYS → AP2112K/AMS1117 → +3.3V
- Check the reverse polarity protection circuit: Q2 (PMOS), R33 (VBAT→GATE), R34 (GATE→GND). Verify this topology is correct for a high-side PMOS reverse polarity protection.
- Check for redundant voltage regulators (AP2112K + AMS1117 both outputting 3.3V?)
- Verify BQ24195 support components match the datasheet (ILIM, NTC, BTST cap, REGN cap, PMID caps, inductor)
- Verify TPS61088 support components
- Verify ESP32-S3 WROOM connections: power, EN, boot strapping (GPIO0), USB D+/D-, I2C, GPIOs
- Check all I2C buses: upstream (MCU → TCA9548A), downstream (TCA channels to JST-SH connectors)
- Verify LED strip path: data line through SN74AHCT125 level shifter, power through fuse + current sense
- Check for missing pull-ups/pull-downs on critical signals
- Verify no floating inputs on ICs
- Check decoupling capacitors on all ICs

#### 2. ERC analysis (use reports/ERC.json)
- 60 violations (all warnings, 0 errors)
- Previous review found 30 warnings, all benign. 30 new warnings appeared — likely from the new ESP32 and power components. Categorize each and determine if any indicate real issues.
- 2 `lib_symbol_issues` (known: USB-C and BQ24195 imported symbol libraries not in config)
- 58 `pin_to_pin` warnings — the previous review found these were mostly from EasyEDA-imported symbols with "Unspecified" pin types. Verify the new ones follow the same pattern.
- This ERC.json file is generated by our 'kiutils' script. There is also a ERC.rpt which I generate through the Kicad PCB editor UI, which sometimes contains different information and is worth checking also. See it here: "hardware/pcb-main/kicad/ERC.rpt"

#### 3. DRC analysis (use reports/DRC.json)
- 36 violations (29 errors, 7 warnings), 0 unconnected nets
- Previous review had 33 violations, all within JLCPCB manufacturing tolerances. Categorize the current 36:
  - 25 clearance violations — determine if these are within JLCPCB capabilities (min 0.09mm clearance, 0.127mm trace, 0.2mm drill)
  - 3 silk overlap — cosmetic only
  - 2 annular width — likely from component footprints (known for TPS61088 thermal vias and USB-C)
  - 2 hole clearance — likely USB-C mounting holes (known)
  - 2 padstack — investigate, these may be new
  - 1 track_dangling — investigate, may indicate a leftover trace from J8 removal
  - 1 connection_width — investigate
- The modified Keystone 1042 footprint (2 NPTH holes deleted) should be checked for any DRC issues
- This DRC.json file is generated by our 'kiutils' script. There is also a DRC.rpt which I generate through the Kicad PCB editor UI, which sometimes contains different information and is worth checking also. See it here: "hardware/pcb-main/kicad/DRC.rpt"

#### 4. BOM / assembly readiness
- Read `hardware/pcb-main/kicad/production/bom.csv` and verify all LCSC part numbers are present
- BT1 (Keystone 1042) is NOT on LCSC — must be hand-soldered. Verify it's excluded from or marked in the JLCPCB BOM.
- D1, R1 (new power LED) need LCSC part numbers
- All new ESP32 sub-sheet components need LCSC part numbers
- Check the previous preflight review (`hardware/pcb-main/kicad/reports/preflight-review.md`) for any substitutions that were made — verify those are still in the schematic

#### 5. PCB layout checks (use pcb-layout-context.json)
- Verify the Keystone 1042 (BT1) is on the back side and within board outline
- Check that BT1 pads don't overlap with any through-hole pads on B.Cu (especially USB-C J7 mounting pins)
- Verify ESP32 antenna keepout zone (X: 121-140.5, Y: 100.9-107.6) has no copper on any layer
- Verify the 18650 cell body (65mm × 18.3mm diameter) won't physically overlap the antenna area in 3D
- Check that all 92 components are placed (extract reported 92 placed, 0 unplaced)
- Look for any components placed outside the board outline

#### 6. New components annotation
22 components lack AI annotations (run `python3 tools/schematic-context/show.py hardware/pcb-main/kicad/motion-play-main.kicad_sch --unannotated` to see the list). These should be annotated for future sessions. The unannotated components are:
- BT1, D1, R1, SW1, U8, U9 (power management sheet — new)
- U3, C50-C55, D5, R50-R53, SW2, SW3 (ESP32 sheet — all new)

DO NOT Use `tools/schematic-context/annotate.py` to write annotations after reviewing. Using that script causes issues with the kicad files, creating a headache for me, so we want to be careful about using it. Propose annotations, but do not try to annotate. Do not edit any of the kicad files directly either. 

### Output format

Produce a structured report similar to `hardware/pcb-main/kicad/reports/preflight-review.md` with:
- Overall verdict (PASS / CONDITIONAL PASS / FAIL)
- Section-by-section findings with severity (blocker / warning / advisory / info)
- Specific action items for anything that needs fixing before ordering
- A clear list of what's safe to ignore vs. what must be fixed
